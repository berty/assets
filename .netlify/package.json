{
  "name": "berty-assets",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pre-build": "node scripts/generate-content.js",
    "build:dev": "npm run pre-build && hugo server --gc",
    "build:prod": "npm run pre-build && hugo --gc --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "glob": "^7.1.5"
  }
}
